/** Production: set `apiBaseUrl` to your API origin if the app and API are on different hosts (e.g. https://api.example.com). Same-origin deployments can use ''. */
export const environment = {
  production: true,
  apiBaseUrl: '',
};
